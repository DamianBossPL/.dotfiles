#!/bin/bash

DIR="$(dirname "$0")/scripts"
SCRIPT=$(ls -p "$DIR" | grep -v / | rofi -i -dmenu)

if [ -z $SCRIPT ]; then exit; fi

SCRIPT_PATH="$DIR/$SCRIPT"
echo "$SCRIPT_PATH" > ~/.last_script

"$SCRIPT_PATH"
