#!/bin/bash

DIR="$HOME/.local/share/soundboard"
SOUND=$(ls -p "$DIR" | grep -v / | rofi -i -dmenu)

if [ -z $SOUND ]; then exit; fi

SOUND_PATH="$DIR/$SOUND"
echo "$SOUND_PATH" > ~/.last_soundboard

~/.local/bin/paplay-mic "$SOUND_PATH"
